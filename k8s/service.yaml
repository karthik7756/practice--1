apiVersion: v1
kind: Service
metadata:
  name: financial-app-service
  labels:
    app: financial-app
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
  selector:
    app: financial-app
